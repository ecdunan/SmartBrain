export const particlesParams = {
  particles: {
    number: {
      value: Math.random() * (+50 - +30) + +30,
      density : {
        enable: true,
        value_area: Math.random() * (+400 - +200) + +200
      }
    }
  }
}
