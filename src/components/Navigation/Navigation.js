import React from 'react';

function Navigation() {
    return (
        <nav style={{display: 'flex', justifyContent: 'flex-end'}}>
            <p className='f3 link dim black underline pa3 pointer mt0'>Sign Out</p>
        </nav>
    );
}

export default Navigation;